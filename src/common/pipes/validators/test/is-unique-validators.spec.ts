import { IsUniqueValidators } from './is-unique-validators';

describe('IsUniqueValidators', () => {
  it('should be defined', () => {
    expect(new IsUniqueValidators()).toBeDefined();
  });
});
